<div class="content-heading">
  <%if @form.form_type == :cpuproduced then%>
    <div class="text-align-right">
      <%= link_to forms_path do%>
        <button class="btn btn-secondary">
          <i class="fas fa-arrow-left text-primary fa-1x"></i>
        </button>
      <%end %>
    </div>

    <div class="col-8"> Editar Formulario CPU Producidas </div>
  <%else %>
    <div class="text-align-right">
      <%= link_to request_forms_index_forms_path do%>
        <button class="btn btn-secondary">
          <i class="fas fa-arrow-left text-primary fa-1x"></i>
        </button>
      <%end %>
    </div>

    <div class="col-8"> Editar Formulario de Solicitud de Componentes </div>
  <%end %>

</div>
<div class="card-body">
  <%= render 'form', form: @form %>
</div>