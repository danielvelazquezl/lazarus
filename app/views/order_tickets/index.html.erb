<div class="content-heading">
  <div> Nota de Pedido </div>
  <div class="ml-auto">
    <%= link_to new_order_ticket_path do %>
      <button class="btn btn-secondary">
        <i class="fas fa-plus text-primary fa-1x"></i>
      </button>
    <% end %>
  </div>
</div>

<div class="card card-body bg-light">
  <%= puts @filterrific.inspect %>
  <%= form_for_filterrific @filterrific do |f| %>
    <div class="form-group row">

      <div class="col-4">
        <label class="font-weight-bold">Buscar</label>
        <%= f.text_field :search_query,
                         :class => 'filterrific-periodically-observed form-control',
                         placeholder: "por cliente y empleado" %>
      </div>


      <div>
        <div class="col-12">
          <label class="font-weight-bold">Encargado</label>
          <%= f.select :with_employee_id,
                       @filterrific.select_options[:with_employee_id],
                       {:include_blank => '- Todos -'},
                       {:class => 'form-control'} %>
        </div>
      </div>

      <div>
        <div class="col-12">
          <label class="font-weight-bold">Ordenar por</label>
          <%= f.select :sorted_by,
                       @filterrific.select_options[:sorted_by],
                       {},
                       {:class => 'form-control'}
          %>
        </div>
      </div>

    </div>

    <div class="ml-auto">
      <div>
        <%= link_to "Limpiar filtros", reset_filterrific_url, :class => 'btn btn-secondary align-middle' %>
      </div>
    </div>

<% end %>
</div>

<div id="results">
  <%= render(
          partial: 'order_tickets/list',
          locals: {order_tickets: @order_tickets}
      ) %>
</div>

<!--<p id="notice"><%= notice %></p>

<h1>Order Tickets</h1>

<table>
  <thead>
    <tr>
      <th>Client</th>
      <th>Ticket number</th>
      <th>Employee</th>
      <th>Pay method</th>
      <th>Date</th>
      <th>Observation</th>
      <th>State</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_tickets.each do |order_ticket| %>
      <tr>
        <td><%= order_ticket.client_id %></td>
        <td><%= order_ticket.ticket_number %></td>
        <td><%= order_ticket.employee_id %></td>
        <td><%= order_ticket.pay_method_id %></td>
        <td><%= order_ticket.date %></td>
        <td><%= order_ticket.observation %></td>
        <td><%= order_ticket.state %></td>
        <td><%= link_to 'Show', order_ticket %></td>
        <td><%= link_to 'Edit', edit_order_ticket_path(order_ticket) %></td>
        <td><%= link_to 'Destroy', order_ticket, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order Ticket', new_order_ticket_path %>-->
