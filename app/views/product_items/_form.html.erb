<%= form_with(model: product_item, local: true) do |form| %>
  <% if product_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_item.errors.count, "error") %> prohibited this product_item from being saved:</h2>

      <ul>
      <% product_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%#= <%= form.text_field :product_id %>
    <%#<%= collection_select(:product,:description,Product.all,:id,:description,prompt: true) %>
    <%= form.select :product_id,
                 options_from_collection_for_select(Product.all, "id", "description", form.object.product_id),
                 prompt: "Select something" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
