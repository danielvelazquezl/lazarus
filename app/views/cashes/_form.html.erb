<%= form_with(model: cash, local: true, html: { id: "cashes_form" }) do |form| %>
  <% if cash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cash.errors.count, "error") %> prohibited this cash from being saved:</h2>

      <ul>
      <% cash.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card card-default">
    <div class="card-body">
      <p class="lead bb">CAJA</p>
      <div class="form-group row">
        <div class="col-1"></div>
          <div class="col">
            <strong>Descripcion:</strong><%= form.text_field :description %>
          </div>
      </div>

    </div>
  </div>
  <div class="row">
    <div class="col"></div>
    <div class="col-12 col-xl-auto">
      <%= form.check_box :state, :checked => false, :style => 'visibility: hidden' %>
      <%= form.submit 'Guardar', class: "btn btn-success" %>
    </div>
  </div>
<% end %>
<script>
  <%= render "validate.js" %>
</script>